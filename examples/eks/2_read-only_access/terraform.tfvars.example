# Copy this file to terraform.tfvars and fill in your values.
# terraform.tfvars is git-ignored and won't be committed.

# Provider
cloudpilot_api_key      = "sk-xxx"
# cloudpilot_api_endpoint = "https://api.cloudpilot.ai"  # default

# EKS Cluster
cluster_name        = "my-eks-cluster"
region              = "us-west-2"
# aws_profile          = ""   # default: non-empty uses this AWS CLI profile
# custom_node_role     = ""   # default: optional custom EC2 instance role name
# skip_restore         = true # default: skip node restore on destroy
restore_node_number = 2

# --- Node Autoscaler Optimization ---
# By default, only the agent is installed without optimization.
# Step 2: Set only_install_agent to false to install node autoscaler, then run `terraform apply`.
# only_install_agent = false
# Step 3: Set enable_rebalance to true to enable rebalancing, then run `terraform apply`.
# enable_rebalance   = true
