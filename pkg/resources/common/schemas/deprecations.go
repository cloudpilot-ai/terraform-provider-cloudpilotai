package schemas

const ProviderSideTemplateDeprecationMessage = "Provider-side template composition is deprecated and will be removed in a future major provider release. Prefer the cloudpilot-ai/eks/cloudpilotai module for template rendering, or pre-render shared fields with Terraform locals and merge()."
